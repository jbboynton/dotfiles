# Mac-specific initialization

# Use GNU sed instead of the system sed. Run brew info gnu-sed for info.
PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"

# Use GNU make instead of the system make. Run brew info make for info.
PATH="/usr/local/opt/make/libexec/gnubin:$PATH"
